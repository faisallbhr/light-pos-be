# ========================
# 🔧 APP CONFIGURATION
# ========================

APP_NAME="# Application name (e.g., "Light POS")"
APP_ENV="# Application environment: development | staging | production"
APP_PORT="# Server port (e.g., 3000)"

# ========================
# 🗄️ DATABASE CONFIGURATION
# ========================

DB_HOST="# Database host (e.g., localhost)"
DB_PORT="# Database port (e.g., 3306)"
DB_USER="# Database username (e.g., root)"
DB_PASSWORD="# Database password"
DB_NAME="# Database name (e.g., light_pos)"

# ========================
# 🔐 JWT CONFIGURATION
# ========================

JWT_ACCESS_SECRET="# Secret key for JWT access token"
JWT_REFRESH_SECRET="# Secret key for JWT refresh token"
JWT_ACCESS_TTL="# Access token TTL in minutes (e.g., 15)"
JWT_REFRESH_TTL="# Refresh token TTL in minutes (e.g., 60)"

# ========================
# 🌐 CORS CONFIGURATION
# ========================

CORS_ALLOWED_ORIGINS="# Allowed origins list, comma-separated without spaces (e.g., http://localhost:3000,http://localhost:5173)"

# ========================
# 🧬 GOOSE MIGRATION CONFIGURATION
# ========================

GOOSE_DRIVER="# Database driver for Goose (e.g., mysql)"
GOOSE_DBSTRING="# Database connection string for Goose"
GOOSE_MIGRATION_DIR="# Directory path for Goose migration files (e.g., ./database/migrations)"
GOOSE_TABLE="# Goose migration version table name (e.g., goose_db_version)"
